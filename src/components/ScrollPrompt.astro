<div class="scroll">
    <p>scroll</p>
    <div class="scroll-line"></div>
</div>

<style>
    .scroll {
            display: flex;
            flex-direction: column;
            align-items: center;
            position: absolute;
            top: 3em;
            right: 3em;
            gap: 0.5em;
            opacity: 1;
            transform: none;
            transition: opacity 1s cubic-bezier(0.16, 0.84, 0.3, 1), transform 1s cubic-bezier(0.16, 0.84, 0.3, 1);
    
            & p {
                font-family: var(--secondary-font);
                text-transform: uppercase;
                font-size: 0.7rem;
                color: rgba(8, 14, 28, 0.4);
                letter-spacing: 0.22em;
    
            }
    
            .scroll-line {
                width: 1px; height: 42px;
                background: linear-gradient(rgb(8, 14, 28), transparent);
                animation: 1.6s ease-in-out 0s infinite alternate none running scroll-cue;
            }
        }
</style>